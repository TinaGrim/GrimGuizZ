# QuizZ .secretscanignore
#
# Intentional, documented dev-only values (NOT production secrets). This is a
# local demo/educational app: the seed teacher account password and the JWT
# signing secret are deliberately well-known defaults so a reviewer/student can
# log in, and both are overridable via env (.env). They are never used in
# production. They are centralized in the single clearly-commented file below;
# all other backend code reads from settings at runtime and contains no
# hardcoded secrets.
backend/app/config.py
