// Deprecated module — domain types now live in src/data/types.ts and the source
// of truth is the QuizZ FastAPI backend (src/api/client.ts). This file is kept
// only so any stray imports don't break the build during the refactor.
export * from "./types";